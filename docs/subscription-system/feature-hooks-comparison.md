# ユーザータイプ別機能制限フック実装の比較

## 📊 現状の実装 vs 実装後の比較

### 🔴 現状の実装（汎用的なフックのみ）

```
┌─────────────────────────────────────────────────────────┐
│                   各コンポーネント                      │
│  (ModelProfile, PhotographerPortfolio, etc.)           │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
         ┌───────────────────────┐
         │   useSubscription()   │
         │  (汎用フック)          │
         │                        │
         │  - checkLimit()        │
         │  - hasFeature()        │
         │  - getCurrentPlan()    │
         └───────────┬─────────────┘
                     │
                     ▼
    ┌────────────────────────────────┐
    │  開発者が手動で機能名を指定    │
    │                                │
    │  ❌ タイプミスのリスク        │
    │  ❌ 機能名を覚える必要        │
    │  ❌ コードが冗長              │
    │  ❌ ユーザータイプ混在        │
    └────────────────────────────────┘
```

#### 現状のコード例

```typescript
// モデル向けコンポーネント内
function ModelPortfolioUpload() {
  const { checkLimit, hasFeature } = useSubscription();
  const [currentCount, setCurrentCount] = useState(0);

  // ❌ 機能名を文字列で指定（タイプミスリスク）
  const portfolioCheck = await checkLimit('portfolioLimit', currentCount);
  
  // ❌ 機能名を文字列で指定
  const canUsePriority = hasFeature('priorityBookingTickets');
  
  // ❌ 機能名を文字列で指定
  const canAccessAnalytics = hasFeature('reviewAnalytics');
  
  // ❌ ユーザータイプに関係ない機能も表示される可能性
  const canManageClients = hasFeature('clientManagement'); // モデルには不要！
}
```

**問題点**:
- 機能名を文字列で指定するため、タイプミスのリスク
- ユーザータイプに関係ない機能もチェックできてしまう
- コードが冗長で読みにくい
- IDEの補完が効かない

---

### 🟢 実装後の状態（ユーザータイプ別専用フック）

```
┌─────────────────────────────────────────────────────────┐
│             モデル向けコンポーネント                    │
│         (ModelProfile, ModelPortfolio, etc.)            │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
         ┌───────────────────────┐
         │  useModelFeatures()   │
         │  (モデル専用フック)    │
         │                        │
         │  ✅ canUploadPortfolio │
         │  ✅ canUsePriority     │
         │  ✅ canAccessAnalytics │
         │  ✅ 型安全              │
         └────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│          カメラマン向けコンポーネント                  │
│    (PhotographerPortfolio, ClientManagement, etc.)      │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
         ┌──────────────────────────┐
         │ usePhotographerFeatures()│
         │ (カメラマン専用フック)    │
         │                           │
         │  ✅ canManageClients      │
         │  ✅ canRemoveWatermark    │
         │  ✅ canAccessCommercial   │
         │  ✅ 型安全                 │
         └───────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│           運営者向けコンポーネント                      │
│    (OrganizerDashboard, Analytics, etc.)                │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
         ┌──────────────────────────┐
         │  useOrganizerFeatures()  │
         │  (運営者専用フック)       │
         │                            │
         │  ✅ canCreateSession      │
         │  ✅ canAccessAnalytics    │
         │  ✅ canUseMarketingTools  │
         │  ✅ 型安全                 │
         └───────────────────────────┘
```

#### 実装後のコード例

```typescript
// モデル向けコンポーネント内
function ModelPortfolioUpload() {
  const { 
    canUploadPortfolioPhoto,  // ✅ 型安全、IDE補完が効く
    canUsePriorityBooking,     // ✅ 型安全
    canAccessReviewAnalytics  // ✅ 型安全
  } = useModelFeatures();
  
  const currentCount = 15;
  
  // ✅ シンプルで読みやすい
  if (!canUploadPortfolioPhoto(currentCount)) {
    return <UpgradePrompt />;
  }
  
  // ✅ モデルに必要な機能だけが提供される
  // ❌ canManageClients は存在しない（コンパイルエラー）
}
```

---

## 📈 具体的な比較例

### 例1: モデルのポートフォリオ写真アップロード制限チェック

#### ❌ 現状の実装

```typescript
function ModelPortfolioUpload() {
  const { checkLimit } = useSubscription();
  const [currentCount, setCurrentCount] = useState(0);
  
  const handleUpload = async () => {
    // ❌ 機能名を文字列で指定（タイプミスリスク）
    const limitCheck = await checkLimit('portfolioLimit', currentCount);
    
    if (!limitCheck.allowed) {
      // ❌ エラーメッセージも手動で作成
      toast.error(`ポートフォリオ上限に達しています（${limitCheck.limit}枚）`);
      return;
    }
    
    // アップロード処理...
  };
}
```

#### ✅ 実装後のコード

```typescript
function ModelPortfolioUpload() {
  const { canUploadPortfolioPhoto } = useModelFeatures();
  const [currentCount, setCurrentCount] = useState(0);
  
  const handleUpload = async () => {
    // ✅ 型安全、IDE補完が効く
    if (!canUploadPortfolioPhoto(currentCount)) {
      // ✅ エラーメッセージも自動生成可能
      return;
    }
    
    // アップロード処理...
  };
}
```

**改善点**:
- コード行数: 10行 → 5行（50%削減）
- タイプミスリスク: あり → なし
- IDE補完: なし → あり

---

### 例2: カメラマンの透かし除去機能チェック

#### ❌ 現状の実装

```typescript
function PhotographerPortfolio() {
  const { hasFeature } = useSubscription();
  
  // ❌ 機能名を文字列で指定
  const canRemoveWatermark = hasFeature('watermarkRemoval');
  
  return (
    <div>
      {canRemoveWatermark ? (
        <ImageWithoutWatermark />
      ) : (
        <ImageWithWatermark />
      )}
    </div>
  );
}
```

#### ✅ 実装後のコード

```typescript
function PhotographerPortfolio() {
  const { canRemoveWatermark } = usePhotographerFeatures();
  
  // ✅ 型安全、IDE補完が効く
  return (
    <div>
      {canRemoveWatermark() ? (
        <ImageWithoutWatermark />
      ) : (
        <ImageWithWatermark />
      )}
    </div>
  );
}
```

**改善点**:
- 機能名の記憶不要
- タイプミス防止
- コードが簡潔

---

### 例3: 運営者の高度分析機能アクセス制御

#### ❌ 現状の実装

```typescript
function OrganizerAnalytics() {
  const { hasFeature } = useSubscription();
  
  // ❌ 機能名を文字列で指定
  const canAccessAdvanced = hasFeature('advancedAnalytics');
  const canUseMarketing = hasFeature('marketingTools');
  
  if (!canAccessAdvanced) {
    return <UpgradePrompt feature="advancedAnalytics" />;
  }
  
  return (
    <div>
      {canUseMarketing && <MarketingTools />}
      <AdvancedAnalytics />
    </div>
  );
}
```

#### ✅ 実装後のコード

```typescript
function OrganizerAnalytics() {
  const { 
    canAccessAdvancedAnalytics,
    canUseMarketingTools 
  } = useOrganizerFeatures();
  
  // ✅ 型安全、IDE補完が効く
  if (!canAccessAdvancedAnalytics()) {
    return <UpgradePrompt />;
  }
  
  return (
    <div>
      {canUseMarketingTools() && <MarketingTools />}
      <AdvancedAnalytics />
    </div>
  );
}
```

---

## 🎯 実装後のメリット

### 1. **型安全性の向上**

```typescript
// ❌ 現状: 文字列なのでタイプミスが検出されない
hasFeature('reviewAnalytics')  // ✅ 正しい
hasFeature('reviewAnalitic')   // ❌ タイプミス（実行時エラー）

// ✅ 実装後: TypeScriptが型チェック
canAccessReviewAnalytics()      // ✅ 正しい
canAccessReviewAnalitic()       // ✅ コンパイルエラー（即座に検出）
```

### 2. **IDE補完の活用**

```typescript
// ❌ 現状: IDE補完が効かない
hasFeature('...')  // 何を入力すればいいか分からない

// ✅ 実装後: IDE補完が効く
useModelFeatures().  // ← ここで補完が表示される
  - canUploadPortfolioPhoto
  - canUsePriorityBooking
  - canAccessReviewAnalytics
```

### 3. **コードの簡潔性**

```typescript
// ❌ 現状: 冗長
const limitCheck = await checkLimit('portfolioLimit', currentCount);
if (!limitCheck.allowed) { ... }

// ✅ 実装後: 簡潔
if (!canUploadPortfolioPhoto(currentCount)) { ... }
```

### 4. **ユーザータイプの分離**

```typescript
// ❌ 現状: モデルでもカメラマン機能が表示される可能性
const canManageClients = hasFeature('clientManagement'); // モデルには不要！

// ✅ 実装後: モデルにはモデル機能だけ
const { canUploadPortfolioPhoto } = useModelFeatures();
// canManageClients は存在しない（コンパイルエラー）
```

### 5. **保守性の向上**

```typescript
// ❌ 現状: 機能名変更時に全箇所を修正
hasFeature('reviewAnalytics')  // 10箇所で使用
// → 'reviewAnalytics' → 'analytics' に変更する場合、10箇所すべて修正が必要

// ✅ 実装後: 1箇所修正で全体に反映
export function useModelFeatures() {
  return {
    canAccessReviewAnalytics: () => hasFeature('reviewAnalytics')
  };
}
// → 機能名変更時は useModelFeatures 内の1箇所だけ修正
```

---

## 📊 実装コスト vs メリット

### 実装コスト
- **時間**: 2-3時間
- **ファイル数**: 1ファイル（`src/hooks/useUserTypeFeatures.ts`）
- **行数**: 約100-150行

### メリット
- **開発効率**: 30-50%向上（コード補完、型チェック）
- **バグ削減**: タイプミスによる実行時エラーを防止
- **保守性**: 機能名変更時の修正箇所を大幅削減
- **可読性**: コードが簡潔で理解しやすくなる

---

## 🎨 実装イメージ図

```
┌─────────────────────────────────────────────────────────┐
│              useSubscription (基盤フック)                │
│  - checkLimit(featureName, currentUsage)                 │
│  - hasFeature(featureName)                                │
│  - getCurrentPlan()                                       │
└────────────────────┬────────────────────────────────────┘
                     │
        ┌────────────┼────────────┐
        │            │            │
        ▼            ▼            ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│useModel      │ │usePhoto      │ │useOrganizer  │
│Features()    │ │grapher       │ │Features()    │
│              │ │Features()    │ │              │
│✅ モデル専用 │ │✅ カメラマン │ │✅ 運営者専用 │
│  機能のみ    │ │  専用機能のみ│ │  機能のみ    │
└──────────────┘ └──────────────┘ └──────────────┘
```

---

## 💡 結論

**現状の実装**: 汎用的だが、使いにくい  
**実装後の状態**: ユーザータイプ別に最適化され、使いやすい

**実装する価値**: ✅ **高い**
- 開発効率が大幅に向上
- バグ削減
- 保守性向上
- コードの可読性向上

**実装コスト**: 2-3時間（比較的低コスト）

**推奨**: 実装を推奨します。長期的な開発効率とコード品質の向上が見込めます。

---

## 📝 **実装完了記録（2025-11-06）**

### **実装完了項目**

1. **useModelFeatures フック** ✅
   - ファイル: `src/hooks/useModelFeatures.ts`
   - ポートフォリオ制限、優先予約チケット、評価分析等の機能フラグ

2. **usePhotographerFeatures フック** ✅
   - ファイル: `src/hooks/usePhotographerFeatures.ts`
   - クライアント管理、透かし除去、商用ライセンス等の機能フラグ

3. **useOrganizerFeatures フック** ✅
   - ファイル: `src/hooks/useOrganizerFeatures.ts`
   - 撮影会作成数制限、高度な分析、マーケティングツール等の機能フラグ

4. **FeatureGate コンポーネント** ✅
   - ファイル: `src/components/subscription/FeatureGate.tsx`
   - 機能制限UI表示コンポーネント

### **次のステップ**

1. 各コンポーネントでの使用例の実装
2. E2Eテストの実装
3. 既存コンポーネントの移行

