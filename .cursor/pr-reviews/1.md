Skip to content
Navigation Menu
ShuheiHasegawa
shutter-hub

Type / to search
Code
Issues
1
Pull requests
1
Zenhub
Actions
Projects
Wiki
Security
3
Insights
Settings
 Open
feat: QR code check-in system (#1)
#2
ShuheiHasegawa wants to merge 4 commits into main from feature/1-qr-code-checkin-system 
+2,169 −39 
 Conversation 18
 Commits 4
 Checks 3
 Files changed 18
Conversation
@ShuheiHasegawa
Owner
ShuheiHasegawa
commented
13 minutes ago
• 
Closes #1

Note

Introduces a complete QR-based check-in/out flow across backend, UI, and i18n.

DB: add checked_in_at, checked_out_at, and slot_id (indexes, comments, and an organizer update RLS policy) via 20251227055024_add_checkin_to_bookings.sql
Server Actions: new checkInToSlot, getCheckInStatus, and getSessionCheckInStatus for recording and aggregating check-in data
Pages/UI:
New check-in route src/app/[locale]/checkin/[slotId]/page.tsx auto-executes check-in and shows result
Organizer panel integrates CheckInManagement with per-slot QR display, print-all, and basic stats
SlotQRCode component renders printable/downloadable QR codes; BookingCheckInStatus shows a user’s status
CertificateGenerator creates shareable attendance images after check-in
Adjust PhotoSessionDetail and OrganizerManagementPanel to embed check-in features and add print:hidden where needed
Styles/i18n: add styles/print.css, scrollbar utilities in globals.css, and checkin translations in messages/en.json and messages/ja.json
Types/deps: extend Booking with slot_id, checked_in_at, checked_out_at; add qrcode.react, html2canvas, file-saver, and related types
Written by Cursor Bugbot for commit 040c26d. This will update automatically on new commits. Configure here.

Summary by CodeRabbit
New Features
QR code-based check-in system for photo session slots—participants scan codes to record arrival and departure times.
Organizers can view per-slot check-in statistics and print slot QR codes for display.
Participants see their check-in status for each booking with timestamps.
Digital certificate generation upon check-in completion, available for download and sharing.
Print-friendly layouts for QR codes and session details.
Full internationalization support for check-in workflows (English and Japanese).
✏️ Tip: You can customize this high-level summary in your review settings.

ShuheiHasegawa added 4 commits 27 minutes ago
@ShuheiHasegawa
feat: チェックインシステムのデータベーススキーマとServer Actionsを追加 
399f45f
@ShuheiHasegawa
feat: QRコード生成とチェックインページを追加 
194ec4a
@ShuheiHasegawa
feat: チェックイン管理UIコンポーネントを統合 
c4a91df
@ShuheiHasegawa
feat: 入場証明書生成、印刷スタイル、多言語対応を追加 
040c26d
@ShuheiHasegawa ShuheiHasegawa self-assigned this 13 minutes ago
@vercel
vercel bot
commented
13 minutes ago
• 
The latest updates on your projects. Learn more about Vercel for GitHub.

Project	Deployment	Review	Updated (UTC)
shutter-hub	Ready Ready	Preview, Comment	Dec 28, 2025 4:09am
@coderabbitai
coderabbitai bot
commented
13 minutes ago
• 
Warning

.coderabbit.yaml has a parsing error
The CodeRabbit configuration file in this repository has a parsing error and default settings were used instead. Please fix the error(s) in the configuration file. You can initialize chat with CodeRabbit to get help with the configuration file.

💥 Parsing errors (1)
⚙️ Configuration instructions
📝 Walkthrough
Pre-merge checks and finishing touches
❌ Failed checks (1 warning)
✅ Passed checks (4 passed)
✨ Finishing touches
Thanks for using CodeRabbit! It's free for OSS, and your support helps us grow. If you like it, consider giving us a shout-out.

❤️ Share
Comment @coderabbitai help to get the list of available commands and usage tips.

@vercel vercel bot deployed to Preview 12 minutes ago
@sonarqubecloud
sonarqubecloud bot
commented
10 minutes ago
Quality Gate Failed Quality Gate failed
Failed conditions
 D Reliability Rating on New Code (required ≥ A)

See analysis details on SonarQube Cloud

 Catch issues before they fail your Quality Gate with our IDE extension  SonarQube for IDE

cursor[bot]
cursor bot reviewed 9 minutes ago
cursor bot
left a comment
This PR is being reviewed by Cursor Bugbot
Details
src/components/checkin/CertificateGenerator.tsx
          height: '1920px',
          position: 'relative',
        }}
      >
@cursor
cursor bot
9 minutes ago
Certificate element hidden prevents html2canvas from working
The certificate template div has className="hidden" which sets display: none. When handleDownload or handleShare calls html2canvas on certificateRef.current, the library cannot properly capture hidden elements. This causes the certificate download and share functionality to fail or produce empty/incorrect images. The element needs to be positioned off-screen or use visibility: hidden instead of display: none for html2canvas to work correctly.

Fix in Cursor Fix in Web

@ShuheiHasegawa	Reply...
src/app/[locale]/checkin/[slotId]/page.tsx
  }

  // チェックイン処理を実行
  const result: CheckInResult = await checkInToSlot(slotId);
@cursor
cursor bot
9 minutes ago
Page refresh triggers unintended check-out action
The checkInToSlot server action is called unconditionally during server-side rendering whenever the page loads. This means a simple page refresh after check-in will trigger check-out, since the logic treats any second page load as a "second scan." Users could accidentally check out by refreshing the page, using browser back/forward navigation, or clicking the check-in link again. The action is not idempotent and causes unexpected state changes on navigation events that users don't associate with scanning a QR code.

Fix in Cursor Fix in Web

@ShuheiHasegawa	Reply...
src/components/qr-code/SlotQRCode.tsx
              </div>
              <div class="info-item">
                <span class="info-label">場所:</span>
                <span>${sessionLocation}</span>
@cursor
cursor bot
9 minutes ago
XSS vulnerability in print popup via unsanitized data
User-controlled data (session.title and session.location) is interpolated directly into HTML strings passed to document.write without escaping. An organizer could set a malicious session title like </h1><script>alert(document.cookie)</script><h1> which would execute JavaScript in the print popup window. While this is primarily self-XSS (the organizer attacks their own view), it could affect co-organizers or administrators who access the print functionality for sessions they didn't create.

Additional Locations (1)
Fix in Cursor Fix in Web

@ShuheiHasegawa	Reply...
coderabbitai[bot]
coderabbitai bot reviewed 9 minutes ago
coderabbitai bot
left a comment
Actionable comments posted: 10

🧹 Nitpick comments (6)
📜 Review details
.cursor/plans/qrコードチェックインシステム実装_75e99267.plan.md

### 提案1: 自動チェックアウト機能

**課題**: 退場時に再度スキャンを忘れる可能性がある**解決策**:
@coderabbitai
coderabbitai bot
9 minutes ago
⚠️ Potential issue | 🟡 Minor

Formatting issue: missing line break.

There's a missing newline between the two sections, causing them to run together: がある**解決策**:.

🔎 Proposed fix
📝 Committable suggestion
🤖 Prompt for AI Agents
@ShuheiHasegawa	Reply...
messages/en.json
Comment on lines +375 to +407
  "checkin": {
    "checkInManagement": "Check-in Management",
    "checkInManagementDescription": "Display and print QR codes for each slot to manage participant entry and exit",
    "slotQRCode": "Slot QR Code",
    "slotNumber": "Slot Number",
    "time": "Time",
    "print": "Print",
    "downloadQR": "Download QR Code",
    "printAll": "Print All",
    "refresh": "Refresh",
    "checkInStatistics": "Check-in Statistics",
    "totalBookings": "Total Bookings",
    "checkedIn": "Checked In",
    "checkedOut": "Checked Out",
    "checkInProgress": "Check-in Progress",
    "loading": "Loading...",
    "noSlotsAvailable": "No slots available",
    "slotNotFound": "Slot not found",
    "slotNotFoundDescription": "The specified slot does not exist or is invalid",
    "checkedInAt": "Checked in at",
    "checkedOutAt": "Checked out at",
    "viewSessionDetails": "View Session Details",
    "backToSession": "Back to Session",
    "checkInStatus": "Check-in Status",
    "status": "Status",
    "completed": "Completed",
    "checkedIn": "Checked In",
    "notCheckedIn": "Not Checked In",
    "checkInInstructions": "Please scan the QR code to check in",
    "downloadCertificate": "Download Certificate",
    "share": "Share",
    "generating": "Generating..."
  },
@coderabbitai
coderabbitai bot
9 minutes ago
⚠️ Potential issue | 🔴 Critical

Fix duplicate key "checkedIn" within the "checkin" object.

Lines 387 and 401 both define "checkedIn": "Checked In" within the same checkin namespace. JavaScript objects cannot have duplicate keys—only the last definition (line 401) will be retained, causing the first one to be silently ignored.

Impact: The statistics label at line 387 will be inaccessible, potentially breaking UI components that reference it.

Solution: Rename the keys to reflect their distinct purposes:

🔎 Proposed fix
Based on learnings, static analysis tools like Biome correctly identified this as a critical issue.

📝 Committable suggestion
🧰 Tools
🤖 Prompt for AI Agents
@ShuheiHasegawa	Reply...
messages/ja.json
Comment on lines +330 to +362
  "checkin": {
    "checkInManagement": "チェックイン管理",
    "checkInManagementDescription": "スロットごとのQRコードを表示・印刷して、参加者の入退場を管理します",
    "slotQRCode": "スロットQRコード",
    "slotNumber": "枠番号",
    "time": "時間",
    "print": "印刷",
    "downloadQR": "QRコードをダウンロード",
    "printAll": "すべて印刷",
    "refresh": "更新",
    "checkInStatistics": "チェックイン統計",
    "totalBookings": "予約数",
    "checkedIn": "チェックイン済み",
    "checkedOut": "チェックアウト済み",
    "checkInProgress": "チェックイン進捗",
    "loading": "読み込み中...",
    "noSlotsAvailable": "スロットがありません",
    "slotNotFound": "スロットが見つかりません",
    "slotNotFoundDescription": "指定されたスロットは存在しないか、無効です",
    "checkedInAt": "入場時刻",
    "checkedOutAt": "退場時刻",
    "viewSessionDetails": "撮影会詳細を見る",
    "backToSession": "撮影会に戻る",
    "checkInStatus": "チェックイン状態",
    "status": "状態",
    "completed": "完了",
    "checkedIn": "チェックイン済み",
    "notCheckedIn": "未チェックイン",
    "checkInInstructions": "QRコードをスキャンしてチェックインしてください",
    "downloadCertificate": "証明書をダウンロード",
    "share": "共有",
    "generating": "生成中..."
  },
@coderabbitai
coderabbitai bot
9 minutes ago
⚠️ Potential issue | 🔴 Critical

🧩 Analysis chain
Fix duplicate "checkedIn" keys in Japanese translations.

Confirmed: ja.json contains duplicate "checkedIn" keys at lines 342 and 356 within the "checkin" section, mirroring the same issue in en.json (lines 387 and 401). The first key appears in the statistics context (after "totalBookings"), and the second in the status context (after "completed"). This causes the second value to overwrite the first in JSON parsing.

Rename the duplicate keys to distinguish the contexts, such as "checkedInCount" and "checkedInStatus" (or equivalent terms appropriate for Japanese), matching the fix applied to en.json.

🤖 Prompt for AI Agents
@ShuheiHasegawa	Reply...
src/app/actions/checkin.ts
Comment on lines +62 to +130
    // チェックイン状態の判定と更新
    const now = new Date().toISOString();

    if (!booking.checked_in_at) {
      // 初回スキャン：入場記録
      const { error: updateError } = await supabase
        .from('bookings')
        .update({ checked_in_at: now })
        .eq('id', booking.id);

      if (updateError) {
        logger.error('Check-in update error:', updateError);
        return {
          success: false,
          message: 'チェックインに失敗しました',
        };
      }

      logger.info('Check-in successful', {
        bookingId: booking.id,
        slotId,
        userId: user.id,
      });

      return {
        success: true,
        message: '入場完了しました',
        type: 'checkin',
        checked_in_at: now,
        checked_out_at: null,
      };
    } else if (!booking.checked_out_at) {
      // 2回目スキャン：退場記録
      const { error: updateError } = await supabase
        .from('bookings')
        .update({ checked_out_at: now })
        .eq('id', booking.id);

      if (updateError) {
        logger.error('Check-out update error:', updateError);
        return {
          success: false,
          message: 'チェックアウトに失敗しました',
        };
      }

      logger.info('Check-out successful', {
        bookingId: booking.id,
        slotId,
        userId: user.id,
      });

      return {
        success: true,
        message: '退場完了しました',
        type: 'checkout',
        checked_in_at: booking.checked_in_at,
        checked_out_at: now,
      };
    } else {
      // 既に完了
      return {
        success: false,
        message: '既にチェックイン・チェックアウトが完了しています',
        type: 'already_completed',
        checked_in_at: booking.checked_in_at,
        checked_out_at: booking.checked_out_at,
      };
    }
@coderabbitai
coderabbitai bot
9 minutes ago
⚠️ Potential issue | 🟠 Major

Potential race condition (TOCTOU) on concurrent check-in attempts.

Between reading the booking state (lines 46-52) and updating it (lines 67-70 or 95-98), another concurrent request could modify the same record. If a user scans the QR code twice quickly (e.g., double-tap), both requests might read checked_in_at: null and both attempt to set it.

Consider using a conditional update with a WHERE clause that includes the expected state, or use database-level locking.

🔎 Proposed fix using conditional update
@ShuheiHasegawa	Reply...
src/app/actions/checkin.ts
Comment on lines +150 to +170
/**
 * スロットのチェックイン状況を取得（運営者用）
 */
export async function getCheckInStatus(
  slotId: string
): Promise<CheckInStatus | null> {
  try {
    const supabase = await createClient();

    // スロット情報を取得
    const { data: slot, error: slotError } = await supabase
      .from('photo_session_slots')
      .select('id, slot_number, start_time, end_time, photo_session_id')
      .eq('id', slotId)
      .single();

    if (slotError || !slot) {
      logger.error('Slot not found:', slotError);
      return null;
    }

@coderabbitai
coderabbitai bot
9 minutes ago
⚠️ Potential issue | 🟡 Minor

🧩 Analysis chain
Add explicit authorization check to verify organizer status.

This function is documented as "運営者用" (for organizers), but lacks an explicit authorization check at the application layer. While Supabase RLS policies will prevent unauthorized users from viewing booking details, the function should validate that the caller is the organizer before executing the query. This is consistent with the pattern used throughout the codebase (e.g., requireAuthForAction() in admin-system.ts and photo-session-lottery.ts) and provides defense-in-depth security.

🤖 Prompt for AI Agents
@ShuheiHasegawa	Reply...
src/components/checkin/CertificateGenerator.tsx
Comment on lines +33 to +34
  const certificateId = `SH-${slot.id.slice(0, 8).toUpperCase()}-${Date.now().toString(36).toUpperCase()}`;
  const certificateUrl = `${baseUrl}/${locale}/checkin/${slot.id}/certificate/${certificateId}`;
@coderabbitai
coderabbitai bot
9 minutes ago
⚠️ Potential issue | 🟠 Major

Certificate ID is ephemeral and changes on every render.

The certificateId is generated using Date.now() which means it will be different on every component render. This creates a QR code pointing to a URL that may not be verifiable or consistent. If the certificate URL is meant to be validated later, consider:

Generating the ID server-side and storing it with the check-in record
Using a deterministic hash based on slot ID + user ID + check-in timestamp
🤖 Prompt for AI Agents
@ShuheiHasegawa	Reply...
src/components/checkin/CertificateGenerator.tsx
Comment on lines +52 to +53
    } catch {
      // エラーハンドリング: 証明書生成失敗
@coderabbitai
coderabbitai bot
9 minutes ago
⚠️ Potential issue | 🟡 Minor

Silent error handling provides no user feedback.

The empty catch block silently swallows errors during certificate generation. Users won't know if generation failed.

🔎 Proposed fix
🤖 Prompt for AI Agents
@ShuheiHasegawa	Reply...
src/components/checkin/CertificateGenerator.tsx
Comment on lines +100 to +108
      <div
        ref={certificateRef}
        className="hidden print:block bg-gradient-to-br from-purple-50 to-pink-50 p-8 rounded-lg"
        style={{
          width: '1080px',
          height: '1920px',
          position: 'relative',
        }}
      >
@coderabbitai
coderabbitai bot
9 minutes ago
⚠️ Potential issue | 🟠 Major

🧩 Analysis chain
The certificate will fail to capture with html2canvas due to display: none.

The element uses className="hidden print:block" which applies display: none to the screen view. html2canvas cannot render elements with display: none because they have no layout in the DOM. The handleDownload() and handleShare() functions call html2canvas() without any mechanism to make the element visible during capture, resulting in blank or zero-sized images.

Add an onclone callback to the html2canvas options to temporarily make the element visible:

const canvas = await html2canvas(certificateRef.current, {
  backgroundColor: '#ffffff',
  scale: 2,
  useCORS: true,
  onclone: (clonedDoc) => {
    const clonedElement = clonedDoc.querySelector('[data-certificate-ref]');
    if (clonedElement) {
      clonedElement.style.display = 'block';
    }
  }
});
Then add data-certificate-ref attribute to the certificate div.

@ShuheiHasegawa	Reply...
src/styles/print.css
Comment on lines +194 to +197
  [data-print-area='checkin-management'] .flex-shrink-0 {
    flex-shrink: 1 !important;
    flex: none !important;
  }
@coderabbitai
coderabbitai bot
9 minutes ago
⚠️ Potential issue | 🟡 Minor

Fix conflicting flex property declarations.

Line 195 sets flex-shrink: 1 but line 196 immediately overrides it with flex: none (equivalent to flex: 0 0 auto), which sets flex-shrink back to 0. This makes line 195 ineffective.

Solution: Remove the redundant flex-shrink declaration since flex: none already sets all flex properties.

🔎 Proposed fix
Based on learnings, static analysis correctly identified this as a shorthand property override issue.

📝 Committable suggestion
🤖 Prompt for AI Agents
@ShuheiHasegawa	Reply...
supabase/migrations/20251227055024_add_checkin_to_bookings.sql
Comment on lines +28 to +43
CREATE POLICY "Organizers can update check-in for their sessions" ON bookings
  FOR UPDATE
  USING (
    EXISTS (
      SELECT 1 FROM photo_sessions
      WHERE photo_sessions.id = bookings.photo_session_id
      AND photo_sessions.organizer_id = auth.uid()
    )
  )
  WITH CHECK (
    EXISTS (
      SELECT 1 FROM photo_sessions
      WHERE photo_sessions.id = bookings.photo_session_id
      AND photo_sessions.organizer_id = auth.uid()
    )
  );
@coderabbitai
coderabbitai bot
9 minutes ago
⚠️ Potential issue | 🟡 Minor

RLS policy creation lacks idempotency - will fail on re-run.

Unlike the column additions which use IF NOT EXISTS, the CREATE POLICY statement will fail if the policy already exists. Consider wrapping in a DO block or dropping the policy first for idempotent migrations.

🔎 Proposed fix for idempotent policy creation
📝 Committable suggestion
🤖 Prompt for AI Agents
@ShuheiHasegawa	Reply...
This branch was successfully deployed
1 active deployment
Merge info
Some checks were not successful
1 failing, 1 neutral, 3 successful checks


failing checks
SonarCloud Code Analysis
SonarCloud Code AnalysisFailing after 1m — Quality Gate failed
neutral checks
Cursor Bugbot
Cursor BugbotCompleted in 3m — Bugbot Review
successful checks
CodeRabbit
CodeRabbit — Review completed
Vercel
Vercel — Deployment has completed
Vercel Preview Comments
Vercel Preview Comments — ✅ No unresolved feedback
No conflicts with base branch
Merging can be performed automatically.

You can also merge this with the command line. 
Still in progress?
@ShuheiHasegawa


Add a comment
Comment
 
Add your comment here...
 
Remember, contributions to this repository should follow our GitHub Community Guidelines.
 ProTip! Add .patch or .diff to the end of URLs for Git’s plaintext views.
Reviewers
@coderabbitai
coderabbitai[bot]
@cursor
cursor[bot]
Still in progress?
Assignees
@ShuheiHasegawa
ShuheiHasegawa
Labels
None yet
Projects
None yet
Milestone
No milestone
Development
Successfully merging this pull request may close these issues.

 [Feature] QRコードチェックインシステム

Notifications
Customize
You’re receiving notifications because you authored the thread.
1 participant
@ShuheiHasegawa
Footer
© 2025 GitHub, Inc.
Footer navigation
Terms
Privacy
Security
Status
Community
Docs
Contact
Manage cookies
Do not share my personal information
Sign in now to use Zenhub