name: 🔒 CodeQL分析

on:
  # mainブランチへのpush時に自動実行
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'package*.json'
      - '.github/workflows/codeql.yml'

  # 手動実行オプション
  workflow_dispatch:

  # スケジュール実行（週1回）
  schedule:
    - cron: '0 0 * * 1' # 毎週月曜日 00:00 UTC

env:
  NODE_VERSION: '20'

jobs:
  analyze:
    name: 🔒 CodeQLセキュリティ分析
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: ['javascript']

    steps:
      - name: 📥 チェックアウト
        uses: actions/checkout@v4

      - name: 🔧 Node.js セットアップ
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: 📦 依存関係インストール
        run: npm ci

      - name: 🔒 CodeQL分析初期化
        uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}
          queries: +security-and-quality

      - name: 🏗️ ビルド（依存関係の解決）
        run: |
          npm run build
        env:
          SKIP_ENV_VALIDATION: true
        continue-on-error: true

      - name: 🔒 CodeQL分析実行
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:${{matrix.language}}"
        continue-on-error: true # 個人開発では警告のみ

      - name: 📊 分析結果サマリー
        if: always()
        run: |
          echo "## 🔒 CodeQL分析結果" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **言語**: ${{ matrix.language }}" >> $GITHUB_STEP_SUMMARY
          echo "- **実行時刻**: $(date)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📋 詳細な結果は GitHub Security タブで確認できます" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🔧 個人開発最適化" >> $GITHUB_STEP_SUMMARY
          echo "- 警告があっても開発を妨げません" >> $GITHUB_STEP_SUMMARY
          echo "- 重要なセキュリティ問題のみ即座に対応が必要です" >> $GITHUB_STEP_SUMMARY

